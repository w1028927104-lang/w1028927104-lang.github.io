**Ubuntu24安装mihomo**

前置：aliyun轻量应用服务器，Ubuntu 24.04

选择了mihomo+metacubexd WebUI方案



这一切都起源于2026年3月14日我第一次尝试部署Openclaw，为了在云端部署Openclaw我第一次购买了云服务，选择了阿里云轻量应用服务器（2vCPU/4GiB-ESSD云盘/50GiB/峰值公网宽带200Mbps）70元/月套餐，在部署过程中遇到了一个尤为重要的问题，国内的服务器访问Github极不方便，对此开始尝试在服务器使用VPN。首先我找到了clash verge的Linux版，但clash verge for Linux仅支持带图形化界面Ubuntu，服务器安装的Ubuntu通常没有图形化界面也不推荐安装，后来尝试了Clash for Linux以及xray和v2ray和对应的Web UI都失败了，我购买的机场需要mihomo内核，所以我开始寻找支持mihomo的Web UI，后来发现了Metacubexd。Metacubexd是mihomo官方发布的Web UI，可惜我没有经验，经历了很多次失败后才知道Metacubexd。尝试了并成功安装Mihomo+Metacubexd后，我觉得应该写一篇Blog来记录一下，后续需要再次安装的话就不需要搜索教程和问AI了。这也是我第一次写Blog。



**开始**

**1.1** 安装Mihomo

Github：[GitHub - MetaCubeX/mihomo: A simple Python Pydantic model for Honkai: Star Rail parsed data from the Mihomo API. · GitHub](https://github.com/MetaCubeX/mihomo)

在发布页寻找最新版Linux安装包，这次选择的是<u>mihomo-linux-amd64-v2-v1.19.21.deb</u>，将安装包下载至本地再上传到服务器，尽量避免服务器直接下载Github资源出现网络问题

在终端使用`cd /"安装包目录"`命令进入安装包目录

执行安装命令`sudo dpkg -i mihomo-linux-amd64-v2-v1.19.21.deb`

验证安装是否成功`mihomo -v`

**1.2** 安装Metacubexd

Github：[GitHub - MetaCubeX/metacubexd: Mihomo Dashboard, The Official One, XD · GitHub](https://github.com/MetaCubeX/metacubexd)

我直接使用了作者提供的安装方式：

`Clone the gh-pages branch`

`git clone https://github.com/metacubex/metacubexd.git -b gh-pages /etc/mihomo/ui`

`Set external-ui in your config`

`external-ui: /etc/mihomo/ui`

 `Update to latest version`

`git -C /etc/mihomo/ui pull -r`

对于服务器访问GitHub问题考虑，或许可以尝试以下方式来代替git clone：

从Github Metacubexd发布页下载最新的文件<u>compressed-dist.tgz</u>(v1.243.0)到本地计算机，然后再上传至服务器，假设上传在服务器root根目录下

在终端执行`tar zxvf /root/compressed-dist.tgz -C /etc/mihomo/ui --strip-components=1`

(`--strip-components=1`：去掉压缩包内的第一层目录，直接把文件放到 `/etc/mihomo/ui` 下)

执行命令`ls /etc/mihomo/ui | grep index.html`验证解压成功

成功标志：输出`index.html`



**2.1** 配置<u>config.yaml</u>文件

<u>config.yaml</u>文件位置在`/etc/mihomo/config.yaml`

（1）在配置文件中添加`external-controller: 0.0.0.0:9090`来启用mihomo外部控制器，9090是Web UI默认端口

（2）在配置文件中添加`external-ui: /etc/mihomo/ui`来告诉mihomo它ui文件的位置，

其余的暂不需要改动，如图：

<img title="" src="file://photos26-2-23/2026-03-23-20-11-03-image.png" alt="" width="486">

现在使用本地计算机浏览器访问`http://"服务器公网IP":9090/ui`就可以打开Metacubexd  UI

**2.2** 添加订阅节点

AI给的方法是执行命令`wget -O /etc/mihomo/config.yaml "你的机场订阅链接"`可以一键写入你的订阅，但不知道为什么我尝试后失败了

所以我使用了手动的方法

<img src="file:///photos26-2-23/2026-03-23-20-18-05-image.png" title="" alt="" width="590">

在本地计算机Clash Verge中打开配置文件

<img src="file:///photos26-2-23/2026-03-23-20-19-18-image.png" title="" alt="" width="425">

在资源管理器中找到你导入订阅链接的文件

编辑这个配置文件

<img title="" src="file:///photos26-2-23/2026-03-23-20-21-40-image.png" alt="" width="617">

添加**2.1**中的两行代码，并注释掉原配置文件中的`external-controller: '127.0.0.1:9090'`

将这个文件命名为`config.yaml`，删除服务器中的`config.yaml`，将更改过的文件上传至服务器替换掉原先的文件

执行命令`systemctl restart mihomo`重新启动mihomo并更新配置

再次访问`http://"服务器公网IP":9090/ui`就可以正常打开带有节点配置的Web UI



**END**

执行命令`curl -I https://www.google.com --proxy http://127.0.0.1:7890`来验证翻墙是否成功

成功标志：返回`HTTP/2 200`状态码





**常用命令：**

`systemctl restart mihomo`重新启动mihomo来加载新配置

`systemctl status mihomo`查看mihomo运行状态

`sudo apt update && sudo apt upgrade -y`更新系统软件包列表




